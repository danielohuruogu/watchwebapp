export const ColourSelect = ({ label, colourChoices }: ColourSelectProps) => {
  console.log('colour select label: ', label)
  console.log('colour select choices: ', colourChoices)
  return (
    <div className="colour-select">
      colour selector goes here somewhere
    </div>
  )
}